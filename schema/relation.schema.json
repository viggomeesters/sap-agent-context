{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SAP Agent Context relation record",
  "type": "object",
  "required": ["id", "subject_id", "type", "target_id", "source_ids"],
  "properties": {
    "id": {"type": "string"},
    "subject_id": {"type": "string"},
    "type": {"type": "string"},
    "target_id": {"type": "string"},
    "source_ids": {"type": "array", "minItems": 1}
  }
}
